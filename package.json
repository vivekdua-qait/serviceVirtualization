{
  "name": "achieve-mocha-js",
  "private": true,
  "scripts": {
    "test": "mocha test/use_case/addToCart_test.js --timeout 200000 --reporter mochawesome --reporter-options reportFilename=report",
    "test-debug": "mocha --debug 'test/use_case/*.js' --timeout 20000"
  },
  "dependencies": {
    "await-sleep": "0.0.1",
    "chai": "^4.2.0",
    "chromedriver": "^2.46.0",
    "mocha": "^5.2.0",
    "mochawesome": "^3.1.1",
    "puppeteer-core": "^1.13.0",
    "randomstring": "^1.1.5",
    "selenium-webdriver": "^3.6.0",
    "unirest": "^0.6.0"
  }
}
